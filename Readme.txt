These three applications were designed to produce a report based on data stored in O365 
and to update a flag in a 3rd-party system based on data stored in O365.

These three applications could have also been written as (web) service. 

Console applications are convenient for scheduling runs on a specific schedule.